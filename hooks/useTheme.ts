/**
 * Hook de conveniencia para acceder al sistema de temas.
 *
 * Uso:
 *   const { colors, isDark, themeMode, setThemeMode } = useTheme();
 *
 * Re-exportado desde ThemeContext para mayor comodidad de importación.
 */
export { useTheme } from '../context/ThemeContext';
